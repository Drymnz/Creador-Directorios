#include <iostream>
#include <string>
using namespace std;
class Petitions{
    public :
        Petitions();
        string getString(string);
        void mayus(string& text);
        bool numString(string);
        int stringToInt(string);
};